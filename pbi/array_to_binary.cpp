#include <iostream>
#include <vector>

using namespace std;

vector<bool> numere;
long long numar_dec;
int n,nr;

int transformare(vector<bool> &arr)
{
    int dr=vector.size()-1;
    for(int i=1;i<=dr;i++)
    {
        
    }
}

int main(void)
{
    cin>>n;
    for(int i=1;i<=n;i++)
    {
        cin>>nr;
        numere.push_back(nr);
    }
}